name: Deploy Site Prod

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  DEPLOY_PATH: /opt/pageviewer/site
  ARCHIVE_NAME: site-prod.tgz

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    environment:
      name: production
      url: https://pageviewer.ru
    concurrency:
      group: pageviewer-site-production
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Check Yandex Metrika
        run: node scripts/check-analytics.mjs

      - name: Archive site
        run: |
          tar \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='.DS_Store' \
            --exclude='site.tgz' \
            --exclude='site-dev.tgz' \
            --exclude='site-prod.tgz' \
            -czf "${{ env.ARCHIVE_NAME }}" . || test -f "${{ env.ARCHIVE_NAME }}"

      - name: Upload archive
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST || secrets.SSH_HOST_DEV }}
          username: ${{ secrets.SSH_USER || secrets.SSH_USER_DEV }}
          key: ${{ secrets.SSH_PRIVATE_KEY || secrets.SSH_PRIVATE_KEY_DEV }}
          source: ${{ env.ARCHIVE_NAME }}
          target: /tmp

      - name: Publish production site
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST || secrets.SSH_HOST_DEV }}
          username: ${{ secrets.SSH_USER || secrets.SSH_USER_DEV }}
          key: ${{ secrets.SSH_PRIVATE_KEY || secrets.SSH_PRIVATE_KEY_DEV }}
          script_stop: true
          script: |
            mkdir -p "${{ env.DEPLOY_PATH }}"
            find "${{ env.DEPLOY_PATH }}" -mindepth 1 -maxdepth 1 ! -name '.well-known' -exec rm -rf {} +
            tar -xzf "/tmp/${{ env.ARCHIVE_NAME }}" -C "${{ env.DEPLOY_PATH }}"
            rm -f "/tmp/${{ env.ARCHIVE_NAME }}"
